import 'package:flutter/material.dart';

class SearchCompletedFormWidget extends StatelessWidget {
  final String query;

  const SearchCompletedFormWidget({
    super.key,
    required this.query,
  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
